# 进展说明：算法代码 ↔ 中文稿 颗粒度对照

> 本机：CPU（torch 2.5.1+cpu），代码按 GPU 级标准写、在 CPU 上验证**机制正确性**。
> 云机：阿里云 ModelScope DSW 单卡 24G，已建好隔离 venv，跑通 M1/M2 并已得决定性结果。
> 本机测试：`./.venv/Scripts/python.exe run_cpu_tests.py` → **19 passed**。
>
> **状态：立意已被单卡实验证实（DocVQA Pareto，csf 全程占优）。** 详见第七节。

## 一、逐节对照（论文 → 代码 → 测试 → 状态）

| 中文稿 | 代码 | CPU 测试 | 状态 |
|---|---|---|---|
| §3.1 符号/拓扑重塑 T_b, T_b⁻¹ (式1) | `topology.py` | `check_topology_roundtrip` | ✅ CPU 验证 |
| §3.2 四互补算子 (式2–5) | `operators.py` | `check_operators_identity_lemma` | ✅ CPU 验证 |
| 附录A 恒等可达引理 | `operators.py`(0.5·Σ) | `check_operators_identity_lemma` / `check_modulation_uniform_is_half_identity` | ✅ 数值坐实（含 π=¼→0.5H 的尺度） |
| §3.3 头级无状态路由 W_g (式6–7) | `router.py` | `check_router_shapes_and_params` | ✅ CPU 验证（无偏置、参数量=D·K·M） |
| §3.3 P^high 频率显著性 (式8) | `router.high_freq_saliency` | （并入下） | ✅ |
| §3.4 τ_b 分位阈值 + 高频豁免 (式9) | `squeeze.build_plan` | `check_squeeze_ratio_and_exemption` | ✅ CPU 验证 |
| §3.4 低频下采样 (式10) | `squeeze.CompressionPlan`（窗口池化） | `check_squeeze_ratio_and_exemption` / `check_controllable_budget_under_ties` | ✅ 稿件式(10)已改述为"网格对齐窗口池化"；式(9)改为严格 top-⌊ρN⌋（并列可控） |
| §3.4 位置重映射 + M-RoPE 一致 | `squeeze`(质心) + `grid_utils` | `check_position_remap` / `check_positions_match_reference_no_compression` | ✅ 与真实 `rope2d.py` 逐元素一致 |
| §3.5 DeepStack 一次决策/一致传播 (式11) | `propagate.py` + `integrate_qwen3vl` | `check_deepstack_consistency` / `check_deepstack_naive_vs_consistent` | ✅ CPU 验证（核心工程点）+ 消融开关 |
| §3.6 香农熵正则 + λ 调度 (式12–13) | `losses.py` | `check_entropy_loss` / `check_lambda_schedule` | ✅ CPU 验证 |
| 可微性说明（选择前向、调制可微） | `module.py` | `check_module_grad_flow` | ✅ 梯度达 W_g 与投影器 |
| §4 实验/消融（含决定性实验） | `scripts/`（见六、七节） | — | ✅ **已 GPU 实测**；稿中表3/4/5 数字待回填 |

## 二、真实模型集成（§3.5 落到 Qwen3-VL forward）

依据 transformers `modeling_qwen3_vl.py`（已核对 main 分支真实 forward）：
- 视觉塔在 ViT 层 `[8,16,24]` 抽特征 → `deepstack_merger_list` → `deepstack_features`（J 级，每级 [N_visual, D]）。
- 注入在 LLM **前 J 个解码层**：`_deepstack_process` 做 `hidden[visual_pos] += deepstack_embeds[layer]`（式 L856）。
- `patch_qwen3vl` / `make_csf_model_forward`（`integrate_qwen3vl.py`）按真实流程写实：
  `get_image_features` → 逐图压缩 → 用同一 plan 压缩各级 deepstack → 重建序列（缩短占位段）→ 由我方 out_positions 造 M-RoPE position_ids（镜像 `get_rope_index_3`）→ 传 `visual_pos_masks` + 压缩后的 `deepstack_visual_embeds` 给 `language_model`。
- 其中**纯张量的逐行重建** `_compress_row` 已在 CPU 单测通过（`check_compress_row_rebuild`）；**整体 forward 替换已在 GPU 上对 transformers 5.9 验证通过**（M1 Stage B：patched generate 正确读出图中文字）。
- DeepStack 消融开关 `module.deepstack_mode`：`consistent`（默认，本文）/ `naive`（DeepStack-unaware，注入前 N_out 原始行→位置错位），用于量化一致传播的必要性。

## 三、需与稿件对齐的颗粒度差异（诚实标注）

1. ~~式10 措辞~~：**已对齐**——稿件式(10) 已改述为"网格对齐 $s\times s$ 窗口平均池化 + 感受野质心位置"；式(9) 改为严格 top-⌊ρN⌋（并列按显著性截断，保证压缩比可控）。
2. ~~实验数字~~：**已开始实测**——决定性实验/帕累托已跑（见七节）；稿件 §4 表3/4/5 待用实测数字回填（用 ≥100 样本，勿用早期 50 样本的噪声值）。
3. **patch 的 batch 假设**：当前 `make_csf_model_forward` 按"每行单图"写；多图/视频需扩展（已在注释标注）。

## 四、一句话总结颗粒度

**方法层（§3.1–3.6 + 附录A）已与中文稿逐式对应、CPU 机制级全绿（19/19）；DeepStack 集成已按真实 forward 写实并在 GPU 上端到端验证；决定性实验已跑、立意被证实。** 即"立意—公式—代码—测试—实测"全链闭环，剩下的是扩大样本/基准把论文表格数字填实。

## 五、云上环境（24G ModelScope DSW · 已配好）

`.external/` 仅本机读源码用，云上不需要；运行时只依赖 venv 内的 transformers/qwen-vl-utils + ModelScope 下载的模型权重。

### 已完成的一次性配置（无需重复，仅作记录）
隔离 venv 建在 `/mnt/workspace/csf_env`，已装 `torch 2.5.1+cu121` + `transformers 5.9.0` + accelerate/qwen-vl-utils/modelscope/pillow/matplotlib。
> 关键教训（已踩过）：不要动 base 环境、不要 `uv sync` 本仓库（会装 CPU torch）；transformers 5.x 要求 torch≥2.4，否则打印 `Disabling PyTorch` 并禁用模型。这些已解决，新实例才需重做。

### 每次重新连上实例（重启/新终端）必做
```bash
source /mnt/workspace/csf_env/bin/activate     # 激活已建好的 venv（最容易忘）
cd /mnt/workspace/cfs_squeeze                  # 仓库根目录（脚本 import 依赖此处）
# 两条核验红线：
python -c "import torch,transformers;print(torch.__version__,torch.cuda.is_available(),transformers.__version__)"  # 期望 cuda=True
python -c "from transformers import Qwen3VLForConditionalGeneration;print('model class OK')"  # 无 Disabling 警告
```
> 若 `which python` 不在 `csf_env` 内，说明没激活 venv——先 `source` 再跑任何脚本。
> 代码更新：`git pull` 即可（`.external/` 已 gitignore，不会拉巨大源码树）。

### 里程碑（M0–M1 已通过）
- **M0 自检** ✅：上面两条核验 + `python run_cpu_tests.py` → 19 passing（runner 按文件路径加载，避免与环境内同名 `tests` 包冲突；务必在仓库根目录跑）。
- **M1 冒烟** ✅：`python scripts/smoke_test_gpu.py --rho 0.25 --stride 2` —— Stage A（真实特征压缩+DeepStack 对齐）与 Stage B（patched generate）均已通过。
- **M2 决定性实验** ✅：见第七节，立意证实。
- **M3 扩展**（进行中/待跑）：InfoVQA、DeepStack 消融、效率（TTFT/吞吐）、训练版（LoRA+learned router）、8B（QLoRA）。

## 六、实验脚本（GPU）

- `scripts/_docvqa_eval.py`：共享评测库——ANLS + 答案字段兼容 + 短答案 prompt + 输出清洗；`load_benchmark`(docvqa/infovqa)；模型/模块加载；`configure_arm`（dense/uniform/csf/csf-naive）。纯函数 CPU 可测。
- `scripts/run_kill_experiment.py`：单点三臂对照（`--benchmark`、`--arms`、`--debug`）。
- `scripts/pareto_sweep.py`：rho×stride 帕累托扫描 → `results/pareto.{csv,json}` + 发表级图 `pareto.{pdf,png}`（accuracy vs. avg visual tokens，dense 上界线，英文注解，色盲安全）。`--from-csv` 仅重绘。
- `scripts/run_all.py`：**一键编排**四个 block（pareto-doc / pareto-info / ablation-ds / confirm），各存 CSV/JSON、自动判读 gap 并标 CONFIRMED。`--quick` 小子集冒烟全流程；`--from-dir` 纯 CPU 重汇总/重绘；`--only` 选块。
> matplotlib 云上已装；缺则 `pip install matplotlib`（装进 csf_env）。

## 七、已确认结果（决定性实验）

**DocVQA Pareto，Qwen3-VL-4B，training-free（能量探针 ‖X̂₄‖₂，stride=2，n=100）：**

| ρ | uniform | csf | gap (csf−uniform) | csf vs dense | csf tokens |
|---|---|---|---|---|---|
| dense | — | 0.9661 | — | — | 3684 |
| 0.5 | 0.8798 | 0.9376 | +0.058 | −0.029 | 2607 (1.41×) |
| **0.35** | 0.8617 | **0.9325** | **+0.071** | −0.034 | 2150 (1.71×) ← 甜点区 |
| 0.25 | 0.8122 | 0.8740 | +0.062 | −0.092 | 1824 (2.0×) |
| 0.15 | 0.7384 | 0.7523 | +0.014 | −0.214 | 1479 (2.5×) |

**结论：** ① csf 每个工作点 **Pareto 占优**（ANLS 更高且 token 更少）；② 甜点区 ρ=0.35，仅掉 0.034 换 1.71× 压缩；③ gap 中等压缩最大、极端(ρ=0.15)塌缩——文档文字占比>15%，可写成有原理的 limitation（建议 ρ≥0.25）；④ 量级以 ≥100 样本为准（早期 50 样本的 +0.147 是噪声）。training-free 即如此，训练版预计更好。

**下一步建议：** 一键跑全套，拿 InfoVQA 曲线 + DeepStack 消融数：
```bash
python scripts/run_all.py --quick        # 先冒烟全流程
python scripts/run_all.py                # 正式：pareto-doc/info + ablation-ds + confirm
```
