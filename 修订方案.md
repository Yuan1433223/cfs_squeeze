# CSF-MoE 论文修订方案

> 目标：发表二三区 SCI。决策前提（已与作者确认）：
> - 算力：可租**单卡**云 GPU（A100/4090 级）。实验走"4B + LoRA + 2~3 benchmark + 消融"的精简路线。
> - 定位：**保留 CSF-MoE 叙事**，但修正定理错误、降权薄理论、补全工程细节、替换不实引用。
> - 环境：本机仅 CPU。代码按标准 GPU 神经网络写（设备无关 / AMP 安全 / 变长 ragged），配套 CPU 测试脚本先验证机制。
>
> 本文档只做"方案与待办"，不含代码实现。作者审定后再进入编码阶段。

---

## A. 致命问题与修正（不改必被拒）

### A1. 模型规格事实错误：Qwen3-VL 没有 3B / 7B
- **现状**：摘要/3.2/4.1 反复写 "Qwen3-VL-3B / 7B"，3.2 写 "Qwen3-VL-7B, D=4096, M=32"。
- **事实**（官方 [GitHub](https://github.com/QwenLM/Qwen3-VL) / [技术报告 2511.21631](https://arxiv.org/abs/2511.21631)）：dense 版本为 **2B / 4B / 8B / 32B**，MoE 版本 30B-A3B / 235B-A22B，均基于 Qwen3 backbone，2025.10.15 起分批发布。
- **修正**：
  - 全文规格改为 **Qwen3-VL-4B（主）/ 8B（备）**。
  - `D=4096, M=32` 实为 **8B** 的 LLM 规格（≈Qwen3-8B）。4B 的 hidden 更小。
  - 重算参数：8B 下 `W_g ∈ R^{4096×128}` = 0.52M 的论述**仍成立**，但要明确归到 8B；4B 单独给数。
  - 工程上 **D、M 一律从模型 config 读取，禁止硬编码**（写进方法描述与代码）。

### A2. "VisualPrism (IEEE 2026)" 查无此文 —— 疑似伪造引用
- 搜索无任何叫 VisualPrism 的工作。**伪造 baseline = 学术诚信问题，必须删除**。
- **替换**为真实、可核验的视觉令牌压缩工作（择 1~2）：
  - GlimpsePrune（真实，arXiv [2508.01548](https://arxiv.org/abs/2508.01548)，Qwen2.5-VL-7B，平均剪枝 92.6%）。
  - EvoPrune / VisionTrim (ICLR 2026) / LVPruning / G-Prune（training-free，便于在单卡上复跑做对比）。

### A3. "Fang et al. (ACL 2025) 频域剪枝 MoE" 对不上
- MoE 剪枝文献里的 "frequency" 多指**专家选择频率（expert-selection / activation frequency）**，非信号处理频域。真实相关：EAC-MoE (ACL 2025, PESF 基于选择频率)、MoE-Pruner 等。
- **修正**：找到确切原文核对；若确为"选择频率"被误读成"频域"，则改写表述或更换引用。**不确认就不要引**。

### A4. 定理 1 "唯一解" 是错的（一行反例可推翻）
- 约束组（eq.13）：`γ₂+γ₄=1, γ₁=γ₂, γ₃=γ₄`，**3 方程 4 未知数**，解为一参数族：
  令 `γ₄=t` ⇒ `(γ₁,γ₂,γ₃,γ₄)=(1−t, 1−t, t, t)`，对**任意 t** 都有 `ΣγₖX̂ₖ ≡ H`。γ=0.5 只是 t=0.5 的对称代表。
- **修正**：删除"唯一非平凡解"，改为如下**正确表述**（见 B 区给出完整改写）。

### A5. 路由 π 与定理 γ 的 2 倍尺度矛盾
- 前向用 softmax 路由 π（`Σ_k π=1`）。均匀路由 π=0.25 时输出 `= Σ0.25·X̂ₖ = 0.25(X̂₁+X̂₂)+0.25(X̂₃+X̂₄) = 0.5·H`，**不是 H**。
- 定理要 H 需要 `Σγ=2`（每个 0.5），那不是概率分布 → π 与 γ 自相矛盾。
- **后果**：4.5 节"ID4 与 baseline 精确到小数点一致"只有在可学习 W_B 吸收 0.5 尺度时成立；而一旦 W_B 能吸收，定理就退化为平凡的"恒等映射可达（up to scale）"。
- **修正**：把定理降级为引理（见 B 区），诚实声明"恒等可达，至多差一个被投影器吸收的常数尺度"，并据此把 ID4 重新定位为 **sanity-check（正确性自检）而非实验发现**。

---

## B. 数学部分的正确改写（替换 3.3.3 / 3.3.4）

### B1. 引理（替换"定理 1"）
> **引理 1（恒等映射可达性 / 无结构性瓶颈）。** 设 `H_{2D,b}` 为低秩隐特征。四个算子满足定义式
> `X̂₂ = H − X̂₁`、`X̂₄ = H − X̂₃`，故其线性张成
> `span{X̂₁,X̂₂,X̂₃,X̂₄} = span{H, G(H), Blur(H)} ∋ H`。
> 因此**存在**缩放系数 `γ`（构成一参数族 `(1−t,1−t,t,t)`，对称代表 `γ=0.5`）使
> `Σₖ γₖ X̂_{k,2D,b} = H_{2D,b}`。在均匀软路由 `π=1/4` 下模块输出为 `½H`，与 `H` 仅差一个常数尺度，可被共享投影器 `W_B`（或一个可学习的逐头标量）精确吸收。

- 价值表述改为：**该基组足以表示恒等，框架不会引入结构性低秩信息瓶颈**；不再宣称"唯一""满秩完备 2×2 正交空间"。
- 诚实补一句"张成仅 3 维"，把"2×2"解释为**两条独立的残差拆分轴（全局/局部、低频/高频）**，而非四象限正交基——审稿人查到也无懈可击。

### B2. ID4 消融重新定位
- 改写 4.5-1：ID4（锁定 π=1/4、关闭压缩）应**等于 LoRA 基线（at most up to 投影器吸收的尺度）**，作为**实现正确性自检**，不作为"完备性的实证证据"。删掉"无可辩驳的实证证明完备性"这类过强措辞。

---

## C. 工程空洞的补全（方法节必须写死）

### C1. 自适应阈值 τ_b（eq.16 当前未定义）
用**目标保留比 ρ 的分位数**定义（可控、可复现，优于含糊的"方差"）：
```
τ_b = Quantile_{1−ρ}( { P^high_{b,i} : i=1..N_b } )
M_{b,i} = 1 if P^high_{b,i} ≥ τ_b  else 0      # 保留高频 token 的比例≈ρ
```
- ρ 为超参（如 0.25/0.5），直接对应"高频豁免比例"，并与下采样 stride s 共同决定总压缩率。
- 备选：`τ_b = mean_i P^high + α·std_i P^high`（α 超参）。二者都给公式，正文选其一、附录给另一。

### C2. 可微性与梯度路径（当前完全未交代）
- **硬掩码不可微属正常**：与标准 MoE top-k 选择一致——选择本身不回传梯度。
- router `W_g` 仍通过两条**可微**路径学习：
  1. 特征调制 `Σ_k π_{m,k} X̂_k` 是可微的（softmax 处处可导）；
  2. 熵正则 `L_ent`。
- 明确写："token 选择为前向决策，梯度经由可微的特征调制与 `L_ent` 传播"；如需对掩码也回传，附录给 **Gumbel-Softmax / straight-through** 备选。

### C3. 下采样 token 的 M-RoPE 位置赋值（卖点的硬约束）
- 保留的高频 token：沿用原始 `(t,h,w)`。
- 被下采样的低频 token：赋以其**池化窗口中心坐标**（构成像素 `(h,w)` 的均值，或左上角），落在**原坐标系**内。
- **诚实表述**：混合后网格变为**非均匀**，但所有位置仍处于原始坐标系，相对几何关系近似保留 —— 比"完全保留对齐"弱，但站得住，且仍优于直接 drop 的 GlimpsePrune。
- 同步更新对 Qwen3-VL 的 RoPE 描述：Qwen3-VL 用 **Interleaved-MRoPE + DeepStack**（非 Qwen2.5-VL 的分组 M-RoPE），全文 M-RoPE 段落据此更新。

### C4. 头级(M) × 逐通道(D) 的映射（当前含糊）
- 写死：`D = M·d`，通道按头分为 M 组、每组 d 维；第 m 头的 d 个通道由 `Σ_k π_{m,k}·X̂_k`（作用于该 d 通道）调制。代码以此实现，测试覆盖。

### C5. 模块插入位置
- 明确插在 **vision-language merger（MLP projector）之后、喂入 LLM 之前**——这是 TTFT/吞吐收益的真实来源。给出数据流图。

---

## D. 叙事降权（保留 CSF-MoE 名号的前提下）

- 保留 "CSF-MoE" 与"条件计算/隐式专家"叙事，但：
  - 把**频率感知自适应令牌压缩**抬为一等卖点（这是真正可测、对文档/OCR 有说服力的贡献）。
  - 把"定理/完备性/正交"这套理论从"核心论证"降为"设计合理性说明（引理级）"，避免过度宣称招黑。
  - 3.2 节那段冗长的"我们这算不算 MoE"的自辩**压缩到 2~3 句**——越长越显心虚。

---

## E. 单卡可行的实验计划（替换 4.1–4.4）

| 项 | 设定 |
|---|---|
| 主模型 | Qwen3-VL-4B-Instruct + LoRA(r=64) on projector + LLM attention |
| 备选 | 8B（若显存允许，4-bit + LoRA） |
| 训练数据 | 小规模 SFT 子集（参考 GlimpsePrune：~20K，如 GQA / DocVQA train 子集），单卡数小时可完成 |
| Benchmark（2~3 个） | **DocVQA**（验证高频豁免，主战场）、**TextVQA**、+1 通用（MMBench 或 POPE） |
| 压缩档位 | stride `s ∈ {2,3,4}`（背景 4×/9×/16×） + 高频保留比 ρ |
| Baselines | ①Full dense 4B ②LoRA(vanilla) ③GlimpsePrune（真实，必要时在 Qwen2.5-VL 上同台对比）④一个 training-free 方法（FastV/VisionZip/G-Prune 之一） |
| 消融 | w/o `L_ent`（验证 constant-trap）、spatial-only、freq-only、uniform-gating(ID4 自检) |
| 效率指标 | TTFT、Throughput(tok/s)、可训练参数量、视觉 token 数；单卡 batch=1 与小 batch |

- **诚实声明**：当前论文里的 76.4 / 84.2 / 83.9→48.2 等是**占位符**，最终必须用真实运行结果替换；83.9→48.2 这类"灾难性悬崖"需实测支撑，不能臆造。

---

## F. 代码与 CPU 测试架构（下一轮编码的蓝图，本轮不写实现）

```
csf_moe/
  config.py          # 从 HF model config 读取 D, M, d；模块超参 (K=4, r, ρ, s, λ schedule)
  topology.py        # T_b / T_b^{-1}：变长(h,w)元数据驱动的 1D<->2D 重塑（支持 ragged）
  operators.py       # 4 个非参数 DSP 算子：global / local / lowfreq(avgpool3x3) / highfreq
  router.py          # 头级无状态路由 W_g (无 bias) -> π[M,K]，head×channel 调制
  squeeze.py         # P^high/P^low、τ_b 分位阈值、硬掩码、bilinear 下采样、位置(h,w)重映射
  losses.py          # L_ent（香农熵探索）+ λ warmup/decay 调度器
  module.py          # CSFMoEModule：整合，设备无关 / AMP 安全 / fp32 数值关键路径
  integrate_qwen3vl.py  # 插入 merger 之后的 hook / wrapper

tests/  (CPU, 全部可在本机跑)
  test_topo_roundtrip.py   # T_b∘T_b^{-1}=identity，多种(h,w)与 ragged batch
  test_identity_lemma.py   # 数值验证 Σ0.5·X̂ₖ ≈ H（引理1，rtol 容差）
  test_router_shapes.py    # π 形状[M,K]、softmax 归一、无 bias、head×channel 映射正确
  test_squeeze_ratio.py    # 给定 ρ,s 的实际压缩率符合预期；高频 token 全保留
  test_position_remap.py   # 下采样 token 的(h,w)落在原坐标系、窗口中心正确
  test_entropy_loss.py     # 均匀分布 L_ent≈0；坍缩分布 L_ent≈2；λ 调度曲线
  test_grad_flow.py        # 调制路径与 L_ent 对 W_g/W_A/W_B 有梯度；掩码不阻断
  test_tiny_end2end.py     # 用小型随机 VLM stub 跑通 前向+反向+压缩，校验序列变短
```
- 工程标准：变长用 `cu_seqlens`/list-of-tensor，避免 padding 浪费；数值敏感处（熵、softmax）走 fp32；所有算子 `device`/`dtype` 跟随输入；带 `torch.no_grad()` 推理路径与训练路径分离。
- CPU 验证目标：**机制正确性**（形状、恒等、压缩率、梯度、位置）；性能/精度大数另在单卡 GPU 跑。

---

## G. 改动优先级清单

1. **[必做·诚信]** 删 VisualPrism；核对/替换 Fang et al.（A2、A3）
2. **[必做·事实]** 3B/7B → 4B/8B，维度从 config 读（A1、C5）
3. **[必做·数学]** 定理 1 → 引理 1，去"唯一"、修 π/γ 尺度、ID4 改自检（A4、A5、B）
4. **[必做·工程]** 补 τ_b 公式、可微性说明、位置重映射、head×channel（C1–C4）
5. **[必做·实验]** 重写为单卡可行方案，占位数字标注待测（E）
6. **[建议]** 叙事降权，压缩自辩段落（D）
7. **[下一轮]** 按 F 编码 + CPU 测试

---

*本方案为编码前的设计冻结依据。作者确认 A–E 后，进入 F 的实现。*
